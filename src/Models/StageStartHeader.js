export default class StageStartHeader {
    constructor(name) {
        this.header = name
    }
    render(context, sprite) {
        // context.drawImage(
        //     sprite.image,
        //     _frame[0], _frame[1], _frame[2], _frame[3],
        //     this.x, this.y, this.sw, this.sh
        // )
    }
}